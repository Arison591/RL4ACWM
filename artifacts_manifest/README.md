# Artifact manifests

Only metadata (logical role, external path supplied at runtime, byte size, SHA-256) belongs here.
Local manifests and all payloads are ignored. Never commit absolute machine paths or credentials.
